[
  {
    "name": "mbr_entry",
    "addr": "0x7C00",
    "note": "BIOS jumped to our boot sector; boot drive is in DL",
    "asserts": [ { "reg": "dl", "eq": "0x80" } ]
  },
  {
    "name": "fs_regions_and_first_cluster",
    "label": "fat12_next",
    "hit": 1,
    "note": "first chain lookup: regions computed, AX holds the first cluster",
    "asserts": [
      { "reg": "ax", "eq": 2 },
      { "mem16": "fat_start",  "eq": 17 },
      { "mem16": "root_start", "eq": 35 },
      { "mem16": "data_start", "eq": 49 }
    ]
  },
  {
    "name": "a20_enabled",
    "label": "gdt_install",
    "note": "enable_a20 ran and verified A20 via the 1 MB alias test",
    "asserts": [ { "mem8": "a20_enabled", "eq": 1 } ]
  },
  {
    "name": "kernel_loaded_at_0x10000",
    "addr": "0x10000",
    "note": "bootloader jumped to the kernel; the file must be resident",
    "asserts": [ { "memcmp": { "addr": "0x10000", "file": "build/kernel.bin", "len": 1536 } } ]
  }
]
